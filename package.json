{
  "name": "react-github-repos",
  "version": "0.0.1",
  "description": "",
  "scripts": {
    "init": "npm install && bower install",
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "jsx app/jsx public/js",
    "watch": "jsx --watch app/jsx public/js",
    "serve": "http-server",
    "install-vendors": "bower install"
  },
  "author": "",
  "license": "MIT",
  "devDependencies": {
    "http-server": "0.8.0",
    "react-tools": "0.13.3",
    "bower": "1.5.3"
  }
}
